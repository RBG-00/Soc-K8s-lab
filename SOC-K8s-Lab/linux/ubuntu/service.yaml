apiVersion: v1
kind: Service

metadata:
  name: ubuntu
  namespace: linux

spec:
  selector:
    app: ubuntu

  ports:
    - name: ssh
      protocol: TCP
      port: 22
      targetPort: 22
      nodePort: 30222

  type: NodePort