apiVersion: v1
kind: Service

metadata:
  name: node-exporter
  namespace: linux

spec:
  selector:
    app: ubuntu

  ports:
    - name: metrics
      port: 9100
      targetPort: 9100

  type: ClusterIP