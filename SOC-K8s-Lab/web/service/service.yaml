apiVersion: v1
kind: Service

metadata:
  name: dvwa
  namespace: web

spec:
  type: NodePort

  selector:
    app: dvwa

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080